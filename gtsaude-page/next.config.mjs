/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
      domains: ['drive.google.com','"drive.usercontent.google.com'], // Allow images from Google Drive
    },
  };
  
  export default nextConfig;
  